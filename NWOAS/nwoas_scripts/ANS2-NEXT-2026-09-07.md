# Internal SSD investigation after S86

FACT: User reached Setup disk selection; internal SSD absent. Prior WinPE
DiskPart enumerated only14GB WINARM2. S86 resolves the core gate, not storage.

Local implementation audit:
- windows_drivers/nvme-driver.md: design plus non-built skeleton.
- Platform/MacMini2020Pkg/MacMini2020.fdf: ANS2 NVMe entries commented TODO.
- Platform/MacMini2020Pkg/AcpiTables/DSDT.asl: no ANS2/NVMe device declaration.
- pcie_emul-d81-usbc-payload.py: USB controllers only, no ANS2 presentation.
- AppleNANDStorageDxe source implements UEFI-side storage, not Windows Storport.
- m1n1 proxy has nvme_init/read/shutdown. These can support a next isolated
  read-only probe; never call against a running guest owning the controller.

Next bounded experiment: preserve D83/S86, enter fresh proxy, inspect actual ANS
ADT MMIO/interrupt/SART data, initialize with existing m1n1 code, read namespace
LBA0/1 into host memory, validate partition-table signatures and CRC. Log only
metadata/hash, no user file contents. Do not issue writes, flushes, format or
partition changes. No target experiment performed during this audit.

After proof of real namespace access, implement and validate Windows-facing
ANS2 storage support. Enumeration and reads must pass before any write path or
installation. Enabling CPU cores is separate work. No installation success yet.
