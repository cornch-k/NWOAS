# S176 review assessment and hardware result

Opus4.8 reviewed ABI and handle cleanup correctly. Its observability finding ignored the companion inspect-app-state.cmd (not in requested four files): caller exit0 means launch only, logs are separately polled; no benchmark completion was inferred. PID label explicitly says cmd pid/session. No-clobber guards are intentional artifact preservation; explicit archived retry is supported operationally. ARM64 MSVC target uses16-bit wchar_t by default; omitting -fshort-wchar in Windows build is not a defect. Race in PowerShell process-status sampling is valid and should be handled in next observer. Tests establish host logic, not Windows API execution; actual launch now independently passed in session1.

Hardware: USERCB launched cmd6580/session1, CinebenchPID5060 present. CPU time stuck3.875s; Application Message window. SYSTEM enumeration cannot see session1 windows, as expected. Separately compiled fixed-path USERREAD.EXE launched a read-only window enumeration in same existing user session, no focus/input. Message says Digicert root certificate missing/libraries broken. Signature inspection: PartialChain plus NotTimeValid; guestUTC2022-05-07, Maxon certvalid2024-07-29..2027-08-16. This establishes a startup trust/time blocker, not render performance. No benchmarkscore.
