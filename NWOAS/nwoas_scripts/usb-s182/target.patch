--- a/src/hv_vm.c
+++ b/src/hv_vm.c
@@ -2523,7 +2523,7 @@
 // unrelated to the banned NC-remap granule hazard). This is BOTH the fix and the decisive diagnostic:
 // if the event ring starts posting real TRBs (IMAN.IP->1, storm stops) the truncation hypothesis is
 // confirmed; if nothing changes it is refuted (-> coherence or refuses-high).
-#define NWOAS_DART_LOWALIAS 1
+#define NWOAS_DART_LOWALIAS 0
 #if NWOAS_DART_LOWALIAS
 // Alias ONE 16KB page: map the truncated-low IOVA (real_pa & 0xffffffff) to the REAL high PA by
 // overwriting the existing flat-window L2 leaf at the truncated index. l1phys = FL1100 DART L1 table.
@@ -3340,70 +3340,75 @@
 /* D81: retain hardware-proven USB DART bypass and translate low payload
  * buffer IPAs in CPU-authored Normal/Data TRBs before the endpoint doorbell.
  * Event Data opaque cookies and immediate data are never addresses. */
-static void nwoas_usbc_payload_alias(u64 op, u32 slot, u32 ep)
-{
-    if (slot != 1 || ep < 1 || ep > 31)
-        return;
-    u64 dcba=read32(op+0x30)|((u64)read32(op+0x34)<<32);
-    u64 dp=nwoas_ipa_to_pa(dcba & ~63UL);
-    if (!dp || !nwoas_in_dram(dp,16)) return;
-    u64 ctx=read64(dp+8);
-    if (!ctx) return;
-    u64 cp=nwoas_ipa_to_pa(ctx);
-    u32 csz=(read32(0x502280010UL)&4)?64:32;
-    if (!cp || !nwoas_in_dram(cp+ep*csz,32)) return;
-    nwoas_cache_maint(cp+ep*csz,32,false);
-    u64 dq=read64(cp+ep*csz+8)&~15UL;
-    if (!dq) return;
-    /* D83: xHC output-context DQ remains at original ring while Windows
-     * retires/zeros old segments. Follow the last observed live Link target
-     * on the next doorbell instead of restarting at the freed first segment. */
-    static u64 saved_ctx[32], saved_dq[32], live_segment[32];
-    if (saved_ctx[ep]!=ctx || saved_dq[ep]!=dq) {
-        saved_ctx[ep]=ctx;saved_dq[ep]=dq;
-        live_segment[ep]=nwoas_ipa_to_pa(dq);
-    }
-    u64 rp=live_segment[ep];
-    static u32 logged=0, link_logged=0;
-    u64 visited[8];
-    for (u32 seg=0;rp && seg<8;seg++) {
-        bool duplicate=false;
-        for (u32 k=0;k<seg;k++)
-            if (visited[k]==rp) duplicate=true;
-        if (duplicate) break;
-        visited[seg]=rp;
-        live_segment[ep]=rp;
-        u64 next=0;
-        for (u32 j=0;j<256 && nwoas_in_dram(rp+j*16,16);j++) {
-            u64 t=rp+j*16, ptr=read64(t);
-            u32 st=read32(t+8),ctl=read32(t+12),type=(ctl>>10)&63;
-            u32 len=st&0x1ffff;
-            if (type==0) break;
-            if (type==6) {
-                /* D82: follow and, if low, alias Link TRB as well as payloads.
-                 * A link already changed to backing PA need not be stage2 mapped. */
-                next=nwoas_in_dram(ptr & ~15UL,16) ? (ptr & ~15UL) :
-                     nwoas_ipa_to_pa(ptr & ~15UL);
-                if (ptr && ptr<0x100000000UL && next && nwoas_in_dram(next,16)) {
-                    write64(t,next);nwoas_cache_maint(t,16,true);
-                }
-                if (link_logged++<16)
-                    printf("[usb-d83] EP%u segment%u link=%lx next=%lx\n",ep,seg,ptr,next);
-                break;
-            }
-            if ((type!=1 && type!=3) || (ctl&BIT(6)) || !ptr || ptr>=0x100000000UL || !len)
-                continue;
-            u64 pa=nwoas_ipa_to_pa(ptr);
-            if (!pa || !nwoas_in_dram(pa,len)) continue;
-            write64(t,pa);
-            nwoas_cache_maint(t,16,true);
-            if (logged++<20)
-                printf("[usb-d83] EP%u segment%u payload %lx -> %lx len=%x\n",ep,seg,ptr,pa,len);
-        }
-        if (!next || !nwoas_in_dram(next,16)) break;
-        rp=next;
-    }
-    sysop("dsb sy");
+/* S175: fresh-boot DART1/SID1 translation, no transfer-pointer rewrites. */
+#include "s175_dart1_translate.h"
+#include "s182_crcr_latch.h"
+static struct nwoas_crcr_latch s182_crcr;
+static u32 s175_read(void *c, uint64_t a) { (void)c; return read32(a); }
+static void s175_write(void *c, uint64_t a, uint32_t v) { (void)c; write32(a,v); }
+static void s175_barrier(void *c) { (void)c; sysop("dsb sy"); }
+static void s175_delay(void *c, uint32_t us) { (void)c; udelay(us); }
+static bool s175_range(void *c, uint64_t a, uint64_t n)
+{ (void)c; return !(a & 0x3fff) && nwoas_in_dram(a,n); }
+static int nwoas_s175_ready;
+static bool s175_check_mapping(const u32 roots[4], u64 ipa)
+{
+    if (ipa >> 38) return false;
+    u32 t = roots[(ipa >> 36) & 3];
+    if (!(t & BIT(31))) return false;
+    u64 l1 = (u64)(t & 0x7fffffff) << 12;
+    if (!s175_range(NULL,l1,0x4000)) return false;
+    u64 e = read64(l1 + ((ipa >> 25) & 2047)*8);
+    if (!(e & 1)) return false;
+    u64 l2 = e & 0xffffffc000UL;
+    if (!s175_range(NULL,l2,0x4000)) return false;
+    u64 leaf = read64(l2 + ((ipa >> 14) & 2047)*8);
+    return (leaf & 1) && ((leaf & 0xffffffc000UL) | (ipa & 0x3fff)) == nwoas_ipa_to_pa(ipa);
+}
+static bool nwoas_usbc_s175_start(u64 op)
+{
+    if (nwoas_s175_ready) return nwoas_s175_ready > 0;
+    u32 roots[4];
+    for (u32 i=0;i<4;i++) roots[i]=read32(FL_DART_BASE+0x210+4*i);
+    u64 rt=S175_XHCI_BASE+(read32(S175_XHCI_BASE+0x18)&~0x1fu);
+    u64 command_ring=0;
+    if(!nwoas_crcr_pointer(&s182_crcr,&command_ring)) {
+        printf("[usb-s182] no complete pre-Run CRCR pointer; Run suppressed\n");
+        nwoas_s175_ready=-1;return false;
+    }
+    printf("[usb-s182] pre-Run CRCR=%lx seen=%u\n",command_ring,s182_crcr.seen);
+    u64 checks[]={0x100000,0xfe9d4000,command_ring,
+        (read32(op+0x30)|((u64)read32(op+0x34)<<32))&~63UL,
+        (read32(rt+0x30)|((u64)read32(rt+0x34)<<32))&~63UL};
+    for(u32 i=0;i<sizeof(checks)/sizeof(checks[0]);i++) {
+        if(checks[i] && !s175_check_mapping(roots,checks[i])) {
+            printf("[usb-s175] mapping mismatch i=%u ipa=%lx; Run suppressed\n",i,checks[i]);
+            nwoas_s175_ready=-1;return false;
+        }
+    }
+    s175_ops ops={NULL,s175_read,s175_write,s175_barrier,s175_delay,s175_range};
+    s175_result result;
+    s175_status st=s175_dart1_sid1_translate(&ops,roots,&result);
+    printf("[usb-s175] setup=%s restore=%s tcr=%x->%x roots=%x,%x,%x,%x wait=%u err=%x@%x:%x\n",
+        s175_status_str(st),s175_restore_str(result.restore),result.before.tcr,
+        result.after.tcr,roots[0],roots[1],roots[2],roots[3],result.flush_wait_us,
+        result.after.error,result.after.error_hi,result.after.error_lo);
+    nwoas_s175_ready=st==S175_OK?1:-1;
+    return st==S175_OK;
+}
+void nwoas_usbc_s175_progress(u64 rt)
+{
+    static u32 count;
+    if(nwoas_s175_ready!=1 || count>=8) return;
+    count++;
+    u64 erst=(read32(rt+0x30)|((u64)read32(rt+0x34)<<32))&~63UL;
+    u64 pa=nwoas_ipa_to_pa(erst),ring=0,evpa=0;
+    if(pa && nwoas_in_dram(pa,16)) ring=read64(pa)&~63UL;
+    if(ring) evpa=nwoas_ipa_to_pa(ring);
+    u32 ctl=0,status=0;
+    if(evpa && nwoas_in_dram(evpa,16)) {nwoas_cache_maint(evpa,16,false);status=read32(evpa+8);ctl=read32(evpa+12);}
+    printf("[usb-s175] irq%u iman=%x erdp=%x:%x evt0_type=%u cc=%u ctl=%x\n",
+        count,read32(rt+0x20),read32(rt+0x3c),read32(rt+0x38),(ctl>>10)&63,status>>24,ctl);
 }
 
 /* D79 read-only descriptor capture: observe GET_DESCRIPTOR(Device) buffer
@@ -3537,10 +3542,20 @@
                     paddr >= 0x502280000UL && paddr < 0x502290000UL) {
                     u64 op = 0x502280000UL + (read32(0x502280000UL) & 0xff);
                     u64 db = 0x502280000UL + (read32(0x502280014UL) & ~3u);
-                    if (paddr >= db+4 && paddr < db+128*4)
-                        nwoas_usbc_payload_alias(op,(paddr-db)/4,val[0]&0xff);
+                    if (paddr == op && (val[0] & 1) && !(read32(op) & 1)) {
+                        if (!nwoas_usbc_s175_start(op)) val[0] &= ~1ULL;
+                    }
                     if (paddr == db + 4 && (val[0] & 0xff) == 1)
                         nwoas_usbc_watch_descriptor(op);
+                }
+                if ((elr >> 48) == 0xffff && (width == 2 || width == 3) &&
+                    paddr >= S175_XHCI_BASE && paddr < S175_XHCI_BASE+0x10000) {
+                    u64 op=S175_XHCI_BASE+(read32(S175_XHCI_BASE)&0xff);
+                    if(paddr==op && width==2 && (val[0]&2))
+                        nwoas_crcr_reset(&s182_crcr);
+                    if(paddr==op+0x18 || paddr==op+0x1c)
+                        nwoas_crcr_write(&s182_crcr,(unsigned)(paddr-op),1u<<width,
+                                         val[0],!(read32(op)&1));
                 }
                 // NWOAS §6-C coherence: BEFORE the guest's FL1100 doorbell write reaches the card
                 // (which makes it DMA-read the command ring), flush the command ring + DCBAA +
--- a/src/hv_exc.c
+++ b/src/hv_exc.c
@@ -609,6 +609,8 @@
     if (armed && pending && global_ie && hv_vgic3_spi_enabled(NWOAS_USB_SPI)) {
         extern void nwoas_usbc_event_coherence(u64 rt, bool before_run);
         nwoas_usbc_event_coherence(rt_base, false);
+        extern void nwoas_usbc_s175_progress(u64 rt);
+        nwoas_usbc_s175_progress(rt_base);
         extern void nwoas_usbc_poll_descriptor(void);
         nwoas_usbc_poll_descriptor();
     }
--- a/Makefile
+++ b/Makefile
@@ -132,7 +132,7 @@
 	firmware.o \
 	gxf.o gxf_asm.o \
 	heapblock.o \
-	hv.o hv_vm.o hv_exc.o hv_vuart.o hv_wdt.o hv_asm.o hv_aic.o hv_virtio.o hv_psci.o hv_vgic.o \
+	hv.o hv_vm.o s175_dart1_translate.o hv_exc.o hv_vuart.o hv_wdt.o hv_asm.o hv_aic.o hv_virtio.o hv_psci.o hv_vgic.o \
 	i2c.o \
 	iodev.o \
 	iova.o \
