
## S158 monitor disconnect and cumulative watchdog follow-up (23:24 KST)

User confirmed both keyboard and trackpad actually worked. Then moving monitor slightly caused black screen. NWOS job5 still completed after initial black screen: Windows alive then. DCP getModeCount drained explicit HPD removed/unplug/poweroff messages and returned True,33,10 afterward. Live display-s158/recommit.py preserves existing framebuffer and geometry, recommitted1280x720 swap6; DCP ACK/poweron/HPD True. User still saw black, then brief BSOD and HDMI NO SIGNAL. Do not label recommit a successful visual recovery.

S158 later bugcheck is0x133,p1=1,p2=0x1e00,p3=fffff803fed19338,p4=0, cumulative DPC/interrupt variant. Instrumentation captured freq24MHz,NS1max96956 ticks(4.0398ms),NS2max756104(31.5043ms),NS1calls76566,NS2calls1377,backlogmax1,CQacks54576,SQ1/1,CQpending0. These are per-execute measurements, not whole-DPC or whole-BHL bounds. Manual debug pauses and display servicing are confounders; cannot assign root cause just from counter maxima or pendingIRQ900/698. Job6 liveness did not complete. No agent reboot was issued during monitor event; host ultimately disconnected following user-reportedBSOD.

TTY Ctrl+C also stopped sibling VUART capture at earlier diagnostic pause. Capture was restarted independently before final bugcheck, so the 0x133/S158 counters were captured; intermediate VUART interval has a gap. Fixed S158 launcher: INT no longer cleans capture, capture --ignore-sigint; cleanup onEXIT/HUP/TERM retained. Default standalone capture remains Ctrl+C stoppable. Syntax/help verified. Prefer SIGINT targeting only run_guest PID for future diagnostic shell entries.

Opus4.8 follow-up completed in claude-s156/opus-s158-result.md. Proposal: defer SQ submission execution to existing poll as a bounded experiment. Not yet implemented/deployed. Review corrections: deferral does NOT acknowledge writes to Windows before physical completion (CQ publication remains afterexecute); NS2max dominance alone does not identify clock stealing as cause; timer accounting/cumulative DPC claim remains unverified. Next use an uninterrupted baseline run before judging a scheduling experiment.
